---
target: the dashboard (admin/src)
total_score: 22
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 3
target_identity: "file:C:\\Users\\pc\\Projects\\majorslair\\admin\\src"
timestamp: 2026-09-21T21-47-40Z
slug: admin-src
closed: true
---
# Critique: Major's Lair admin dashboard (admin/src) — second run

Method: dual-agent. Detector clean (1 advisory: login-page grid). Zero app console errors. No overlay (Chrome blocks https->localhost); measured in-page instead.

## Design Health Score
| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of system status | 3 | Scan state lives only on Overview; one 6s completion toast |
| 2 | Match system / real world | 2 | Live Scoring rules still shows raw keys + placeholder descriptions; 90d vs "Last 90 days" |
| 3 | User control and freedom | 2 | Escape discards an in-progress member edit silently; no undo for bulk role |
| 4 | Consistency and standards | 2 | Activity log leads with X handle; points en-US vs credits browser locale |
| 5 | Error prevention | 2 | Give role inherits Members filters, which now persist across navigation |
| 6 | Recognition over recall | 2 | Low-activity rows have no route to the evidence; threshold lives on Scoring |
| 7 | Flexibility and efficiency | 2 | No column sorting; no multi-select; filtered links need a full reload |
| 8 | Aesthetic and minimalist | 2 | 58px titles outrank every number; 16 stat pairs; 535 post cards |
| 9 | Error recovery | 3 | Diagnosis tool excellent; errors are ephemeral toasts of raw backend text |
| 10 | Help and documentation | 2 | Help is hover-only titles; rule descriptions are placeholders in production |
Total: 22/40 (Acceptable). Previous run: 26/40.

## Design Specificity Verdict
Specific in language, generic in form. Authored: monthly-round checklist, decision column, cost estimate. Generic: 58px display chrome, glow brand mark, orbiting sparkle, login marketing. Scoring rules is a DB dumper; Tracked posts is 23 pages of cards titled by tweet IDs.

Measured: detector 0 anti-patterns + 1 advisory; 0 console errors; no desktop overflow; rows correctly role+tabindex; every control has an accessible name. Contrast: token #5d6863 = 3.20-3.41:1 everywhere it appears (ranks, pagination, sidebar spend, scan-card, panel counters). Smallest text 9px (sidebar brand line, "N rules" counters). At 900px the members table hides 41% including the row-action column.

## Priority Issues
- [P0] Give role inherits Members filters; URL-persisted filters (added last round) now survive navigation, so the sidebar link and Overview step 4 land on a stale filter. Reward can target the removal list. Fix: state audience above the controls, no inherited filters by default, sidebar resets the view.
- [P1] Rule descriptions never reach production: descriptions are written only on row creation and never updated. Fix: refresh from code on startup.
- [P1] Low-activity report has no link to a member's evidence and mixes "never linked" with "went quiet" while claiming it is safe to act on.
- [P1] Cost: 12x range headline, three vocabularies, locale mismatch (points en-US vs credits browser), dead `- (x ? 0 : 0)` in the low bound.
- [P2] 23 filter options in one strip; `text-transform: capitalize` mangles labels ("At Or Below Threshold"); `.depth-custom` loses specificity and renders "CUSTOM".

## Persona Red Flags
Alex: no sorting anywhere; no multi-select; pasted filtered link needs a reload; two "Preview" buttons.
Sam: row role="button" contains link + 3 icon buttons (invalid nesting, ~125 tab stops); estimate dialog focuses first checkbox so cost is never announced, no aria-live on change; 108-option native multi-select with "hold Ctrl"; Shield vs ShieldOff differ by one stroke and one deactivates.
Major: cannot read the rules that decide removals; two low-activity populations (46 vs 71); Escape discards edits; audit dominated by IDs and shows no-op changes.

## Minor Observations
Scan reports empty state points at the old reset location. Pausing a post has no confirmation or success message. Leaderboard bars scale to rank 1. Toast covers the Link member button. Login screen markets to one internal user. Sign-out button 29x24. 

## Questions
1. Why is the removal list the only table with no way to open the proof?
2. What if "who am I about to change" were an object you build and name?
3. What survives if you delete every element whose job is to feel like a control centre?
